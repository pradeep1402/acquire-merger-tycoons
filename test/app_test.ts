import { assertEquals } from "assert";
import { describe, it } from "testing/bdd";
import { createApp } from "../src/app.ts";

describe("", () => {
  it("", () => {
    // createApp();
    assertEquals(1, 1);
  })
})
